extends layout

block content
    div(class='container')
        div(class='row')
            div(class='col s12')
                h5= title
            div(class='col s6 offset-s3') 
                if loggedIn
                    a(class='waves-effect waves-light btn-large', href='/logout') Logout
                else  
                    a(class='waves-effect waves-light btn-large', href='/login') Login with Azure Active Direcotry
